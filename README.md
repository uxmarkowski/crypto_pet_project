# Crypto Helper 2

Flutter-приложение для просмотра цены криптовалют и свечных графиков в реальном времени с переключаемым источником данных (Binance / Bybit).  
Проект сделан как pet-проект, с чистой архитектурой по слоям, DI, репозиториями, обработкой ошибок и предсказуемым стейт-менеджментом через стримы.

---

## ✨ Возможности

- Список криптовалют
- Модуль свечных графиков
- Загрузка данных из удалённого API через HTTP-клиент
- Поддержка нескольких провайдеров данных:
  - **Binance**
  - **Bybit**
- Централизованная обработка ошибок приложения (Flutter errors + zone errors)

---

## 🧱 Архитектура и подход

Проект построен по принципам “слоистой” архитектуры:

- **Presentation**: экраны + VM/State  
  Используется `StreamBuilder` и состояние вида `initial/loading/loaded/error` (sealed/union модель).
- **Domain**: интерфейсы репозиториев (контракты)
- **Data**: реализации репозиториев и удалённые data sources
- **Core/App**: конфигурация API, HTTP-клиент, обработка ошибок, DI

Ключевые идеи:
- зависимости создаются в одном месте (**DI контейнер**) и пробрасываются через **Provider / MultiProvider**
- источник данных выбирается через `ApiConfig` (например, `BinanceApiConfig` или `BybitApiConfig`)
- сетевой слой изолирован через `AppHttpClient` (реализация на `Dio`)
- ошибки Flutter и ошибки зоны отлавливаются и прокидываются в общий обработчик

---

## 🧩 DI контейнер и переключение API

DI инициализируется один раз до `runApp()`:

- создаётся HTTP клиент, который инициализируется базовым URL из `ApiConfig`
- в зависимости от окружения (`BybitApiConfig` / `BinanceApiConfig`) поднимаются разные data sources
- репозитории получают data sources через конструктор

Пример: `DiContainer.initDependency(ApiConfig apiConfig)` создаёт зависимости и формирует список `providers` для `MultiProvider`.

---

## 🛡️ Обработка ошибок

Запуск приложения выполняется в защищённой зоне:

- `runZonedGuarded(_runAppZoned, AppErrorController.setError)`
- `FlutterError.onError` перенаправляет ошибки Flutter в `AppErrorController`
- `AppErrorHandler` оборачивает `App` и может показывать fallback UI для ошибок

Это позволяет единообразно ловить:
- Flutter framework errors
- async/zone errors (например, из Future/Stream)

---

## 📦 Технологии

- Flutter / Dart
- Provider (DI / прокидывание зависимостей)
- Dio (HTTP клиент)
- Streams + StreamBuilder (state management)
- Разделение на слои: presentation / domain / data

---

## 🚀 Быстрый старт

> Требования: установленный Flutter SDK.

```bash
flutter pub get
flutter run
